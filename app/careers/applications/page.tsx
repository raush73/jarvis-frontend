"use client";

import { CareersComingSoon } from "@/components/careers/CareersComingSoon";

/**
 * Phase 7B.1 navigation placeholder only. The Applications feature (pipeline hub
 * + detail working screen + status transitions) is implemented in Phase 7B.5.
 */
export default function CareersApplicationsPage() {
  return (
    <CareersComingSoon
      title="Applications"
      subtitle="Internal application pipeline"
      section="Applications"
    />
  );
}
