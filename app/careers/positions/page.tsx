"use client";

import { CareersComingSoon } from "@/components/careers/CareersComingSoon";

/**
 * Phase 7B.1 navigation placeholder only. The Positions feature (internal role
 * catalog hub + detail + CRUD) is implemented in Phase 7B.2.
 */
export default function CareersPositionsPage() {
  return (
    <CareersComingSoon
      title="Positions"
      subtitle="Internal role catalog"
      section="Positions"
    />
  );
}
